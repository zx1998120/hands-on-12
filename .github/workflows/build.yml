name: Build

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out repository
      uses: actions/checkout@v2
      
    - name: Execute build.sh script
      run: |
        cd /Users/zhangxin/Desktop/hands-on-12
        bash build.sh

